/**
 * Seat And Room outbound layer.
 * Interfaces (ports) for external services.
 * Parameters and return values use only domain types or JDK types.
 */
package org.cleancoders.seatandroom.outbound;
